{
  "name": "front",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy",
    "preview": "vite preview",
    "copy": "npx cpx \"dist/**\" ../back/dist"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "vite": "^6.3.1"
  }
}
